# donnees/config/experiences/<id>/experience.yml
# Bac à sable d'expérience (SAI).

id: cours4
description: world model + MPC + observateur + APK

modele_monde:
  journal: artefacts/datasets/episodes_cours4_modele_mix_v2_signauxhash.jsonl
  champ_latent: signaux_hash
  # optionnel (mais utile dès maintenant)
  latent_cli: signaux_percus_hash_v1

arene:
  # id d'arène (raccourci) ou chemin .yml
  id: cours4_tiny_planification

agent:
  id: planif_mpc_observateur_tabulaire

latent: signaux_percus_hash_v1

generation:
  episodes: 100
  max_ticks: 2000
  seed: 123
  niveau_bruit: null

sorties:
  run_dir: artefacts/runs
  journal: journal.jsonl
  capture_stdout: true

notes:
  - "Ce fichier est un gabarit minimal."
