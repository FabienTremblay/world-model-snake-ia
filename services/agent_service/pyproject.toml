# services/agent_service/pyproject.toml
[project]
name = "agent-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0",
  "numpy>=1.24",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
